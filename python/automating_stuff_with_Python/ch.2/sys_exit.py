import sys

while True:
	print ('Type exit to exit')
	response = input()
	if response == 'exit': 
		sys.exit()
	print("you typed: " + response + ".")

	# Also note that "break" will also end the code....so......yyyyaaa
	